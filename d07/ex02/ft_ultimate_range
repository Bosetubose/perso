int		ft_ultimate_range(int **range, int min, int max)
{
	int i;
	
	if (min > max)
	{
		**range = NULL;
		return (0);	
	}
	if (min < max)
	{
		**range = (int**)malloc(4 * (min - max));
		i = 0;
		while (i < min - max)
			range[0][i++] = min++;
	}
	return (min - max);
}
